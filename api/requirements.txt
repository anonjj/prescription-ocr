# Dependencies for the OCR REST API server
# Install with:  pip install -r api/requirements.txt

flask>=3.0.0
torch>=2.0.0
opencv-python-headless>=4.8.0   # headless = no GUI, sufficient for server use
numpy>=1.24.0
editdistance>=0.6.0
